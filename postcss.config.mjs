const config = {
  theme: {
    extend: {
      backgroundImage: {
        'radial': 'radial-gradient(circle, var(--tw-gradient-stops))',
      },
    },
  },
  plugins: ["@tailwindcss/postcss"],
};

export default config;
